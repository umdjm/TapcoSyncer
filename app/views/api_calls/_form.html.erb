<%= form_for(@api_call) do |f| %>
  <% if @api_call.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_call.errors.count, "error") %> prohibited this api_call from being saved:</h2>

      <ul>
      <% @api_call.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
